import { BrowserRouter, Routes, Route } from 'react-router-dom';
import React from 'react';

function Router() {
  return (
    <BrowserRouter>
      <Routes>
        <Route />
      </Routes>
    </BrowserRouter>
  );
}

export default Router;
