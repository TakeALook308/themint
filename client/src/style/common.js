import styled from 'styled-components';

export const Container = styled.main`
  max-width: 1024px;
  min-height: calc(100vh - 261px);
  margin-left: auto;
  margin-right: auto;
  position: relative;
  padding-top: 68px;
`;
